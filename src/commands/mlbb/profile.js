/**
 * Alias in engleza pentru /profil — aceeasi comanda, acelasi cod.
 * Multi jucatori scriu reflex "/profile", asa ca merge si asa.
 */
import { buildProfileCommand } from './profil.js';

export default buildProfileCommand('profile');
